@model DirectorViewModel

<h2>Delete Director</h2>

<p>Are you sure you want to delete the director with the following details?</p>

<div>
    <h4>Title: @Model.FullName</h4>
    <p>Release Year: @Model.Nationality</p>
    <p>Genre: @Model.Birthday</p>
</div>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-group">
        <a asp-controller="Director" asp-action="DeleteConfirmed" class="btn btn-primary mx-1">Delete</a>
        <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
    </div>
}
